#!/usr/bin/env python
"""This module contains the setup for the installation of the helper modules for the liver project."""

# All arguments are defined in `setup.cfg`.
import setuptools


setuptools.setup()
